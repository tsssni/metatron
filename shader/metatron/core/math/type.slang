implementing math;

namespace mtt {
    using namespace math;
    public typedef Quaternion<f32> fq;
    public typedef Quaternion<f64> dq;
    public typedef Complex<f32> fc;
    public typedef Complex<f64> dc;
}
